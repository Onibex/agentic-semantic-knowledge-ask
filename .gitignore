# Internal authoring aids — NOT shipped to the published manual.
platform/docs/_authoring/

# Python bytecode
__pycache__/
*.py[cod]

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Claude Code — internal tooling stays out of the public repository. The skills
# carry no credentials, but they map our deployment surface: that a dev box runs
# on HTTP without TLS, where the internal runbook lives, Security Group and
# Keycloak redirect details. None of that serves a reader; together it hands a
# stranger a sketch of how we deploy.
.claude/
