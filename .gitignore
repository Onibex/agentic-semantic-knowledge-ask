# Internal authoring aids — NOT shipped to the published manual.
platform/docs/_authoring/

# Deploy tarballs built by platform/scripts/package-remote.sh
*.tar.gz

# Python bytecode
__pycache__/
*.py[cod]

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Claude Code — internal tooling stays out of the public repository. The skills
# carry no credentials, but they describe internal operational detail that
# serves no reader of this repository.
.claude/

# Databricks AI Tools drops ~580 files of its own product documentation here
# (twice — once for itself, once for Gemini CLI) the first time it runs. None of
# it is ours and none of it belongs to a reader of this repository.
.databricks/
.gemini/
